@extends('layouts.app')
@section('content')
@php
    $selectedTab = in_array(request('tab'), ['configuration','files','terminal'], true) ? request('tab') : 'configuration';
    $parentPath = $path === '.' ? null : (dirname($path) === '.' ? '.' : str_replace('\\', '/', dirname($path)));
@endphp
<div class="mx-auto max-w-7xl px-5 py-10" x-data="{tab:@js($selectedTab)}" @if($pending) x-init="setTimeout(() => location.reload(), 3000)" @endif>
    <div class="flex flex-wrap items-end justify-between gap-4"><div><a class="text-sm text-cyan-600 dark:text-cyan-300" href="{{ route('sites.show',$site) }}">Back to site</a><h1 class="mt-2 text-3xl font-semibold">Remote tools</h1><p class="mt-2 text-sm text-slate-500 dark:text-slate-400">{{ $site->domain }} / operations execute through {{ $site->server->name }}</p></div>@if($pending)<span class="rounded-full bg-cyan-50 dark:bg-cyan-400/10 px-3 py-1 text-sm text-cyan-600 dark:text-cyan-300">Remote work in progress</span>@endif</div>
    @if($errors->any())<div class="mt-5 rounded-xl border border-rose-200 dark:border-rose-400/20 bg-rose-50 dark:bg-rose-400/10 p-4 text-sm text-rose-700 dark:text-rose-200">{{ $errors->first() }}</div>@endif
    <div class="mt-8 flex gap-2 overflow-x-auto border-b border-slate-200 dark:border-white/10">@foreach(['configuration'=>'PHP & Nginx','files'=>'File manager','terminal'=>'Command console'] as $key=>$label)<button @click="tab='{{ $key }}'" :class="tab==='{{ $key }}'?'border-cyan-400 text-slate-900 dark:text-white':'border-transparent text-slate-500 dark:text-slate-400'" class="border-b-2 px-4 py-3 text-sm">{{ $label }}</button>@endforeach</div>

    <div x-show="tab==='configuration'" class="mt-6 space-y-6">
        <div class="grid gap-6 lg:grid-cols-2">
            <form method="POST" action="{{ route('site-configurations.store',$site) }}" class="panel">@csrf<input type="hidden" name="type" value="nginx"><h2 class="font-semibold">Nginx virtual host</h2><p class="mt-2 text-sm text-slate-500 dark:text-slate-400">Generated from constrained settings, tested before reload, and restored automatically if validation fails.</p><label class="mt-4 block text-sm">Maximum request body, MB<input class="field" type="number" name="client_max_body_mb" min="1" max="1024" value="100"></label><label class="mt-4 flex gap-2 text-sm"><input type="checkbox" name="static_cache" value="1" checked>Cache static assets for 30 days</label><label class="mt-3 flex gap-2 text-sm"><input type="checkbox" name="include_www" value="1">Include www hostname</label><button class="button-primary mt-5">Apply Nginx settings</button></form>
            <form method="POST" action="{{ route('site-configurations.store',$site) }}" class="panel">@csrf<input type="hidden" name="type" value="php"><h2 class="font-semibold">PHP {{ $site->php_version }} FPM pool</h2><p class="mt-2 text-sm text-slate-500 dark:text-slate-400">A dedicated site pool runs as www-data with its own socket and bounded process limits.</p><div class="grid grid-cols-2 gap-3"><label class="mt-4 block text-sm">Memory MB<input class="field" type="number" name="memory_limit_mb" value="256"></label><label class="mt-4 block text-sm">Max children<input class="field" type="number" name="max_children" value="10"></label><label class="block text-sm">Upload MB<input class="field" type="number" name="upload_max_mb" value="100"></label><label class="block text-sm">POST MB<input class="field" type="number" name="post_max_mb" value="100"></label></div><label class="mt-3 block text-sm">Execution seconds<input class="field" type="number" name="max_execution_time" value="60"></label><label class="mt-4 flex gap-2 text-sm"><input type="checkbox" name="display_errors" value="1">Display errors in responses</label><button class="button-primary mt-5">Apply PHP settings</button></form>
        </div>
        <section class="panel"><h2 class="font-semibold">Configuration revisions</h2><div class="mt-4 divide-y divide-slate-100 dark:divide-white/5">@forelse($configurations as $configuration)<div class="flex flex-wrap items-center justify-between gap-4 py-3"><div><span class="uppercase">{{ $configuration->type }}</span> v{{ $configuration->version }} <span class="ml-2 text-sm capitalize {{ $configuration->status==='failed'?'text-rose-600 dark:text-rose-300':'text-slate-500 dark:text-slate-400' }}">{{ $configuration->status }}</span>@if($configuration->failure_reason)<p class="mt-1 max-w-2xl text-xs text-rose-600 dark:text-rose-300">{{ $configuration->failure_reason }}</p>@endif</div>@if(in_array($configuration->status,['active','superseded'],true))<form method="POST" action="{{ route('site-configurations.rollback',$configuration) }}">@csrf<button class="text-sm text-amber-600 dark:text-amber-300">Restore as new revision</button></form>@endif</div>@empty<p class="py-5 text-sm text-slate-500 dark:text-slate-400">No managed configuration revisions.</p>@endforelse</div></section>
    </div>

    <div x-cloak x-show="tab==='files'" class="mt-6 space-y-6">
        <section class="panel"><div class="flex flex-wrap items-center justify-between gap-4"><div><h2 class="font-semibold">/var/www/{{ $site->domain }}/{{ $path === '.' ? '' : $path }}</h2><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">All paths are resolved beneath this site's root, including symlink targets.</p></div><div class="flex gap-2">@if($parentPath !== null)<a class="button-secondary" href="{{ route('sites.remote',['site'=>$site,'path'=>$parentPath,'tab'=>'files']) }}">Parent</a>@endif<form method="POST" action="{{ route('site-files.store',$site) }}">@csrf<input type="hidden" name="action" value="list"><input type="hidden" name="path" value="{{ $path }}"><button class="button-primary">Refresh</button></form></div></div>
            <div class="mt-5 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="text-slate-500 dark:text-slate-400"><tr><th class="pb-3">Name</th><th>Type</th><th>Size</th><th>Permissions</th><th></th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-white/5">@forelse($listing as $entry)@php($entryPath=$path==='.'?$entry['name']:$path.'/'.$entry['name'])<tr><td class="py-3">@if($entry['type']==='directory')<a class="text-cyan-600 dark:text-cyan-300" href="{{ route('sites.remote',['site'=>$site,'path'=>$entryPath,'tab'=>'files']) }}">{{ $entry['name'] }}/</a>@else{{ $entry['name'] }}@endif</td><td class="text-slate-500 dark:text-slate-400">{{ $entry['type'] }}</td><td class="text-slate-500 dark:text-slate-400">{{ $entry['type']==='file'?Number::fileSize($entry['size']):'-' }}</td><td><form method="POST" action="{{ route('site-files.store',$site) }}" class="flex items-center gap-2">@csrf<input type="hidden" name="action" value="chmod"><input type="hidden" name="path" value="{{ $entryPath }}"><input type="hidden" name="current_path" value="{{ $path }}"><input class="field mt-0 w-20 py-1 font-mono text-xs" name="mode" value="0{{ $entry['mode'] ?? '644' }}" pattern="0[0-7]{3}" title="{{ $entry['owner'] ?? 'owner unknown' }}"><button class="text-xs text-cyan-600 dark:text-cyan-300">Set</button></form></td><td><div class="flex justify-end gap-3">@if($entry['type']==='file')<form method="POST" action="{{ route('site-files.store',$site) }}">@csrf<input type="hidden" name="action" value="read"><input type="hidden" name="path" value="{{ $entryPath }}"><input type="hidden" name="current_path" value="{{ $path }}"><button class="text-cyan-600 dark:text-cyan-300">Edit</button></form><form method="POST" action="{{ route('site-files.store',$site) }}">@csrf<input type="hidden" name="action" value="download"><input type="hidden" name="path" value="{{ $entryPath }}"><input type="hidden" name="current_path" value="{{ $path }}"><button class="text-cyan-600 dark:text-cyan-300">Prepare download</button></form>@endif<form method="POST" action="{{ route('site-files.store',$site) }}">@csrf<input type="hidden" name="action" value="zip"><input type="hidden" name="path" value="{{ $entryPath }}"><input type="hidden" name="destination" value="{{ $entryPath }}.zip"><input type="hidden" name="current_path" value="{{ $path }}"><button class="text-cyan-600 dark:text-cyan-300">Zip</button></form>@if(str_ends_with($entry['name'], '.zip'))<form method="POST" action="{{ route('site-files.store',$site) }}">@csrf<input type="hidden" name="action" value="unzip"><input type="hidden" name="path" value="{{ $entryPath }}"><input type="hidden" name="destination" value="{{ $path }}"><input type="hidden" name="current_path" value="{{ $path }}"><button class="text-cyan-600 dark:text-cyan-300">Unzip</button></form>@endif<form method="POST" action="{{ route('site-files.store',$site) }}" onsubmit="return confirm('Delete this path from the server?')">@csrf<input type="hidden" name="action" value="delete"><input type="hidden" name="confirmed" value="1"><input type="hidden" name="path" value="{{ $entryPath }}"><input type="hidden" name="current_path" value="{{ $path }}"><button class="text-rose-600 dark:text-rose-300">Delete</button></form></div></td></tr>@empty<tr><td colspan="5" class="py-8 text-center text-slate-500 dark:text-slate-400">Refresh to load this directory.</td></tr>@endforelse</tbody></table></div>
        </section>
        @if($editor)<section class="panel"><h2 class="font-semibold">Edit {{ $editor }}</h2>@if($readOperation)<form method="POST" action="{{ route('site-files.store',$site) }}" class="mt-4">@csrf<input type="hidden" name="action" value="write"><input type="hidden" name="path" value="{{ $editor }}"><input type="hidden" name="current_path" value="{{ $path }}"><textarea class="field min-h-80 font-mono text-xs" name="content" spellcheck="false">{{ $readOperation->result }}</textarea><button class="button-primary mt-4">Queue save</button></form>@else<p class="mt-4 text-sm text-slate-500 dark:text-slate-400">The file read is pending.</p>@endif</section>@endif
        <div class="grid gap-6 lg:grid-cols-2"><form method="POST" enctype="multipart/form-data" action="{{ route('site-files.store',$site) }}" class="panel">@csrf<input type="hidden" name="action" value="upload"><input type="hidden" name="current_path" value="{{ $path }}"><h2 class="font-semibold">Upload file</h2><input class="field" name="path" placeholder="{{ $path==='.'?'filename.txt':$path.'/filename.txt' }}"><input class="field" type="file" name="file"><button class="button-primary mt-4">Queue upload</button></form><form method="POST" action="{{ route('site-files.store',$site) }}" class="panel">@csrf<input type="hidden" name="action" value="mkdir"><input type="hidden" name="current_path" value="{{ $path }}"><h2 class="font-semibold">Create directory</h2><input class="field" name="path" placeholder="{{ $path==='.'?'storage/imports':$path.'/new-directory' }}"><button class="button-primary mt-4">Queue directory</button></form></div>
        <form method="POST" action="{{ route('site-files.store',$site) }}" class="panel" x-data="{fileAction:'rename'}">@csrf<input type="hidden" name="current_path" value="{{ $path }}"><h2 class="font-semibold">Advanced file operation</h2><div class="mt-4 grid gap-3 md:grid-cols-2"><label class="text-sm">Action<select class="field" name="action" x-model="fileAction"><option value="rename">Rename or move</option><option value="chmod">Change permissions</option><option value="zip">Create ZIP</option><option value="unzip">Extract ZIP</option><option value="write">Create text file</option></select></label><label class="text-sm">Source or file path<input class="field" name="path" placeholder="{{ $path==='.'?'path/to/file':$path.'/file' }}"></label><label x-cloak x-show="['rename','zip','unzip'].includes(fileAction)" class="text-sm">Destination<input class="field" name="destination" placeholder="path/to/destination"></label><label x-cloak x-show="fileAction==='chmod'" class="text-sm">Mode<input class="field" name="mode" value="0644" pattern="0[0-7]{3}"></label><label x-cloak x-show="fileAction==='write'" class="text-sm md:col-span-2">Contents<textarea class="field min-h-40 font-mono text-xs" name="content"></textarea></label></div><button class="button-primary mt-4">Queue operation</button></form>
        <section class="panel"><h2 class="font-semibold">Transfers and operations</h2><div class="mt-4 divide-y divide-slate-100 dark:divide-white/5">@foreach($operations as $operation)<div class="flex flex-wrap justify-between gap-3 py-3 text-sm"><span>{{ $operation->action }} / {{ $operation->path }} <b class="ml-2 capitalize">{{ $operation->status }}</b>@if($operation->failure_reason)<small class="ml-2 text-rose-600 dark:text-rose-300">{{ $operation->failure_reason }}</small>@endif</span>@if($operation->action==='download'&&$operation->status==='successful')<a class="text-cyan-600 dark:text-cyan-300" href="{{ route('site-files.download',$operation) }}">Download {{ Number::fileSize($operation->size) }}</a>@endif</div>@endforeach</div></section>
    </div>

    <div x-cloak x-show="tab==='terminal'" class="mt-6 space-y-6">
        <section class="panel"><h2 class="font-semibold">Audited command console</h2><p class="mt-2 text-sm text-slate-500 dark:text-slate-400">Commands run as www-data in the current release. Allowed programs: php, composer, git, npm, node, ls, pwd, cat, and tail. Shell operators, absolute paths, and parent traversal are rejected.</p><form method="POST" action="{{ route('terminal.store',$site) }}" class="mt-5 flex gap-3">@csrf<input class="field mt-0 font-mono" name="command" placeholder="php artisan about"><button class="button-primary">Run</button></form></section>
        <div class="space-y-4">@forelse($commands as $command)<article class="overflow-hidden rounded-2xl border border-slate-200 dark:border-white/10 bg-slate-100 dark:bg-black/40"><div class="flex flex-wrap justify-between gap-3 border-b border-slate-200 dark:border-white/10 px-5 py-3 text-sm"><code>$ {{ $command->command }}</code><span class="capitalize">{{ $command->status }} @if($command->exit_code!==null)/ exit {{ $command->exit_code }}@endif</span></div><pre class="max-h-96 overflow-auto whitespace-pre-wrap p-5 text-xs leading-5 text-slate-600 dark:text-slate-300">{{ $command->output ?: 'Waiting for worker...' }}</pre></article>@empty<div class="panel text-center text-slate-500 dark:text-slate-400">No commands have been run.</div>@endforelse</div>
    </div>
</div>
@endsection
