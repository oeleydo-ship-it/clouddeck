@extends('layouts.app') @section('content')<div class="mx-auto max-w-md px-5 py-20"><form method="POST" action="{{ route('password.update') }}" class="panel p-8">@csrf<input type="hidden" name="token" value="{{ $token }}"><h1 class="text-2xl font-semibold">Choose a new password</h1>@if($errors->any())<p class="mt-4 text-sm text-rose-600 dark:text-rose-300">{{ $errors->first() }}</p>@endif<label class="mt-6 block text-sm">Email<input class="field" type="email" name="email" value="{{ $email }}" required></label><label class="mt-4 block text-sm">New password<input class="field" type="password" name="password" minlength="12" required></label><p class="mt-1 text-xs muted">At least 12 characters.</p><label class="mt-4 block text-sm">Confirm password<input class="field" type="password" name="password_confirmation" required></label><button class="button-primary mt-6 w-full">Reset password</button></form></div>@endsection
