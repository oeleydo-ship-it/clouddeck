@extends('layouts.app') @section('content')<div class="mx-auto max-w-md px-5 py-16"><form method="POST" class="rounded-3xl border border-slate-200 dark:border-white/10 bg-white dark:bg-white/[.04] p-8">@csrf<h1 class="text-2xl font-semibold">Create your account</h1>@if($errors->any())<p class="mt-4 text-sm text-rose-600 dark:text-rose-300">{{ $errors->first() }}</p>@endif@foreach([['name','Name','text'],['email','Email','email'],['password','Password','password'],['password_confirmation','Confirm password','password']] as [$name,$label,$type])<label class="mt-4 block text-sm">{{ $label }}<input name="{{ $name }}" type="{{ $type }}" required class="mt-2 w-full rounded-xl border border-slate-200 dark:border-white/10 bg-white dark:bg-slate-900 px-4 py-3"></label>@endforeach<button class="button-primary mt-6 w-full">Create account</button></form></div>@endsection
