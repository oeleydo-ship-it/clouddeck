@extends('layouts.app') @section('content')<div class="mx-auto max-w-5xl px-5 py-10"><p class="text-sm text-cyan-600 dark:text-cyan-300">Secure access</p><h1 class="mt-1 text-3xl font-semibold">SSH keys</h1>@if($errors->any())<div class="mt-5 rounded-xl border border-rose-200 dark:border-rose-400/20 bg-rose-50 dark:bg-rose-400/10 p-4 text-sm text-rose-700 dark:text-rose-200">{{ $errors->first() }}</div>@endif @if(session('download_key'))<a class="button-primary mt-5 inline-block" href="{{ route('ssh-keys.download',session('download_key')) }}">Download private key now</a><p class="mt-2 text-xs text-amber-600 dark:text-amber-300">This download is available once.</p>@endif<div class="mt-8 grid gap-6 lg:grid-cols-2"><form method="POST" action="/ssh-keys/generate" class="panel">@csrf<h2 class="font-semibold">Generate managed key</h2><p class="mt-2 text-sm text-slate-500 dark:text-slate-400">CloudDeck securely generates and encrypts a 4096-bit RSA key used by provisioning workers.</p><label class="mt-5 block text-sm">Key name<input class="field" name="name" placeholder="CloudDeck primary"></label><button class="button-primary mt-5">Generate key</button></form><form method="POST" action="/ssh-keys" class="panel">@csrf<h2 class="font-semibold">Upload public key</h2><label class="mt-5 block text-sm">Key name<input class="field" name="name"></label><label class="mt-4 block text-sm">OpenSSH public key<textarea class="field min-h-28 font-mono text-xs" name="public_key"></textarea></label><button class="button-primary mt-5">Upload key</button></form></div><div class="mt-6 space-y-3">@forelse($keys as $key)<article class="panel flex items-center justify-between gap-4"><div><h2 class="font-medium">{{ $key->name }}</h2><p class="mt-1 break-all font-mono text-xs text-slate-500 dark:text-slate-400">{{ $key->fingerprint }}</p><p class="mt-1 text-xs {{ $key->private_key ? 'text-emerald-600 dark:text-emerald-300' : 'text-slate-500 dark:text-slate-400' }}">{{ $key->private_key ? 'Managed key' : 'Public key only' }}</p></div><form method="POST" action="/ssh-keys/{{ $key->id }}">@csrf @method('DELETE')<button class="text-sm text-rose-600 dark:text-rose-300">Delete</button></form></article>@empty<div class="panel text-center text-slate-500 dark:text-slate-400">No SSH keys yet.</div>@endforelse</div></div>@endsection
